# Far services and roofs · 230

This lane changes only the repeated distant building containers. It preserves all source component geometry and uses private container collections so foreground services remain exact.

The complete middle-left service route at depth103 is removed. Other600mm barrels remain native size; their former tapered terminals are replaced by full-size curved pipes leading into alternating broad receiver boxes or circular bolted wall flanges. Every route declares its barrel and wall destinations.

Top-front band components at or above the corresponding actual roof are removed. Intermediate bands and all roof slabs remain. Each current building gets an unequal condenser cabinet on seated skids, intake louvers, fan guard geometry and two small roof service penetrations. Selected end-wall ladders have native rungs, wall standoffs and returns onto the roof. The module handles the parent’s18-root infill layout dynamically.

API: `tools/far_services_roofs_230.py::apply(scene)`, after the facade230 module. New geometry uses existing215 distant ink ownership. Root must update the native110 contact visibility after all layout changes, preserving225 lost-sill cleanup.

`scene.blend` and `audit.json` retain the initial12-root CPU proof. `scene-18-roots.blend` and `audit-18-roots.json` test the expanded layout. `native-prefabs.blend` exports the editable18-root service/roof collections with dependencies. `preservation-check.json` checks original objects against source229. These are CPU assembly checks, not visual approval; root owns the combined actual camera render.
