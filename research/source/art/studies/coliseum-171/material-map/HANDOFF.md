#171 native material receiver map

Saved168 was inspected without mutation or rendering. The8px ray grid records3057 landmark hits across550 object/material slots. `material-map.json` contains representative exact receivers and diagnostic socket options; `receiver-summary.json` gives the full observed face map. Original attached coordinates, world normals, and material slots are retained in `material-receivers.json`.

The strongest distinction to preserve is between downward-facing soffits, outward trim, and vertical exposed core. Band08 profile3 at(1913,580) is a true downward soffit(normalZ≈−1). Band10 profile1 and profile3 at(2160,628)/(2176,628) are near-vertical broken faces still using151 core. New167 and selected crown faces instead use165 actual-light core. Thus neither the object name profile3 nor a dark pixel alone identifies a protected underside.

All current152-labeled age groups bind to `162 Unequal broad facade age`; the complete group graph is included. Bypassing the group via its linked Source color isolates that field while preserving older pigment/deposits/AO/palette. Final noise-driven MIX factors are catalogued separately from their nonlinear upstream thresholds. Some catalogued mixes are light contributions: inspect blend/second color before suppressing a factor. No blanket shader change or causal visual diagnosis is authorized by this inventory.

Tools are isolated here: map_native_171.py, map_groups_171.py, summarize_171.py.170 remains parked and all canonical scenes/geometry/materials unchanged. Root’s locked-light diagnostic images should select the causal layer before any new material study.

## Selected primary-deposit hook

Root selected a chroma-only diagnostic on the primary age operation. Exact hook: group162 Unequal broad facade age → Mix (Legacy), label152 primary connected sheltered deposit, MULTIPLY. Factor comes from Math.210.Value; Color1 from Group Input.Source color; Color2 is linear(.55,.54,.62,1). Output feeds Mix (Legacy).001 primary exposed midtone. Preserve all factor masks/strength and secondary nodes. The158 redistribution node Mix (Legacy).004 feeds Color2 of Mix (Legacy).002, not the primary deposit. `primary-deposit-hook.json` records exact sockets and protected secondary nodes. No material was edited in this mapping task.
