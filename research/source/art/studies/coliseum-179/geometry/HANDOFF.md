#179 isolated crown candidate — held

Native proof is complete, but this candidate is not ready to integrate. In the actual painted/clay views the left crest is shortened substantially while the main peak remains close to the baseline. That is not convincing evidence of a broader connected core/return hierarchy. Existing native ink also leaves a tall exposed dark remnant above the shortened crest; it has not been hidden to improve the proof.

All four targets remain closed with zero robust crossings. The explicitly permitted seven rear/core source-face splits, actual peak anchor coordinates, and niche/lower protected triangle gates are documented. Existing nonzero tiny triangles were measured in float64, rather than discarded because BMesh reported zero. Retained normal storage delta is at most0.000575.

However, the final stronger source-containment certificate finds30 off-source samples on U4L, nearest-source residuals0.076–1.081mm. The other three targets have none. This supersedes the preliminary gate pass. No tolerance was increased, and no further repair or iteration is requested by this handoff.20,484 unrelated entries and all existing material graphs are unchanged.

Compare comparison-painted-4x.png and comparison-clay-4x.png. Candidate.blend is isolated diagnostic output only.173 remains the retained scene. No reusable integration API is supplied for a held candidate.
