# Alley weathering198

Native first-facade material and repeated alley damage study from beam-rust197. The reference property is connected medium-scale coating wear on broad interiors, supplemented by actual local recesses, apertures, broken panel edges and finite cracks. The first building receives the full finish pass, including its lower-left base wall and rear portal post. Damage repeats as a varied editable vocabulary on visible facade skins farther along the alley.

Reference IDs: UCL-01 (the creator's original ChatGPT Images2.5 artwork), UP-03 and DP-03. Existing references retain their credits. No reference raster is projected into the scene.

`tools/alley_weathering_rollout_198.py::apply(scene)` is deterministic on a fresh197-derived scene. It copies all affected materials privately, retains original panel meshes, and privately duplicates only selected shared kit instances. Services, beams, bolts, landmark, camera, lighting, palette and layout remain fixed. Native geometry losses use irregular closed cutters; there are no painted false holes. Panel selection verifies actual camera visibility.

The existing perimeter ink may need a local refresh at edge-loss notches; inspect the integrated proof before accepting this pass. The module retains existing material graphs and native ink collection membership. The two-pass192 compositor remains enabled.

Root schedules `tools/alley_weathering_proof_198.py` after reserving the GPU. This is a native4K render, with inherited compositing and ink, not an alternate shader preview. No score or user approval is claimed before rendered inspection.

## Current V2 CPU candidate

The V1 full native proof and matching crops are under `held-v1/`. Its connected facade finish was retained after root and independent agent inspection. V2 changes the overly similar lower-left recesses into differently placed/shaped/deep groups, uses seeded irregular crack paths, and distributes19 native damaged skins across4 first-facade,10 farther-left and5 visibly exposed right panels. Hidden right quota fillers are not created. The rear portal post uses the existing field for lighter worn-metal intervals so weathering remains visible against its dark base.

`preservation.json` compares saved/reopened V2 to197:21,348 original objects retained,537 original material graphs unchanged, no non-target geometry changes, all original local transforms preserved, and camera/render state unchanged. Existing source objects/materials that become unused are explicitly retained, avoiding Blender save-time pruning. Selected shared kit instances use private collections; the master source geometry remains unchanged.

The current V2 candidate has not been independently GPU-rendered. Root will inspect it in the combined203 native proof with149/156/161/192 ink guards explicitly installed. The existing root-level image is still V1; it must not be described as V2. No95 score or user approval is claimed.

## V3 missing-edge completion

Two additional native side-edge chunk losses are now included on separately ray-verified visible sheets, one on each side of the street: left layout_access.007 at projected4K(576,1576), right layout_transition.036 at(3555,1644). These cross the real sheet boundary and remove through-thickness geometry. They are additional to the19 damaged-panel events and do not replace the inspected primary recess composition. Eight old contact-ink points that would span the newly missing edges were hidden locally on a private drawing copy; the rest of the drawings remain unchanged. No failed/hidden top-edge candidate is counted as a missing chunk.
