#208 native recess finish

Replay `tools/alley_recess_refinement_208.py::apply(scene)` on205 before final save. This private-copies14 marked198recess meshes to add existing inner-edge marks and four substrate materials; no vertex/topology/transform edits. Original materials and meshes retained with fake users. The saved thin-ink selection collection is unlinked from scene hierarchy, retained by fake user and dedicated ink lineset.

Exclude all other198cracks/impacts and older145holes. Exact targets/edges in audit.json. Beauty retains existing surfaces; native ink receives a0.25base floor-to-wall contour selector. Existing general linesets ignore edge marks; landmark126selector remains its own collection.208 creates no new mesh overlays or modeled surfaces.

Material response bypasses only198's final0.34/0.32/0.32darkening, keeps upstream lighting/color, adds mild warm exposure, darker real sidewalls and two scales of irregular mineral flecks. Existing native pit geometry stays fixed.

Actual proof and review required; no user approval implied.
