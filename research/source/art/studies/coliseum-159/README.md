#159 — Broken cornice and attached weathering

Current native working candidate, with161 foreground visibility correction. Independent coliseum scores:95 silhouette/scale,95 structural depth,93 damage plausibility,93 surface richness,93 painted integration,94 linework. The95-per-axis objective and final user approval remain open.

## What changed

-157V3 subtracts real material from eight upper-right cornice pieces and the attached U10 sill: short unequal stone ends and an offset stepped cavity. The ninth potential course target was unchanged by the cutter and kept exactly.
-158V2 concentrates the existing secondary pigment into one cooler attached spandrel region, with a quiet warm shoulder. Existing receiver/deep-value masks remain active; no added noise layer.
-160 repairs U10L's folded cap/end return:292 robust crossing pairs to0, closed topology and no zero-area faces. Protected positions/material coordinates remain exact; one custom normal differs by2.55e-6 after serialization. U15R remains held separately.
-161 suppresses63 segments from three exact foreground beam sources proven behind the right fascia at all three segment samples. Visible rails, seams and perimeter remain.149/156 native visibility guards stay active.

The3× bottom splatter from147 and all current alley, sky, ground, city and haze work are preserved.

## Native delivery and evidence

`scene.blend`, `kit.blend`, `main-4k.png` (3840×2885), and `prototype/review-159.html`. The corrected native render took119.35seconds. The pre161 native scene/render remain in `regression/pre161/`.

`preservation.json` compares20,490 recursive scene/instance objects. Only ten landmark mesh targets and authorized private material assignments change; camera, lights, transforms and unrelated geometry/normals are fixed. Some unused superseded material datablocks are discarded on proof reload; their former users are explicitly listed and within the authorized replacement scope. Retained old material graphs are unchanged.

Fresh scene/kit audit verifies all3,533 landmark objects under evaluated geometry, normals, transforms and material assignment. No linked IDs; nine full-scene file images packed. Six actual native kit proofs cover whole camera, new failure and repaired return in painted/clay modes. Shadow-facing clay has weak fine-contact contrast; it is not a global contact certificate.

The independent first full critique in `critic.json` predates161 and reports the then-pending foreground issue. `art/studies/coliseum-161/visibility/correction-review.json` resolves that issue after all ten meaningful corrected-difference regions were inspected. The correction preserves all20,490 native fingerprints/material graphs, but is not pixel-identical:1,172pixels exceed4RGB against uncorrected159; six in the landmark region, maximum8, are minor residual shading variation. Do not describe those residuals as proven ordinary render noise.

`delivery-manifest.json` pins the finite incremental recipe from156 plus the validated160 native payload. Standalone rendering needs no external artwork: run the three embedded149/156/161 visibility texts first, or trusted project-file auto-execution. Do not change global security preferences. The kit appends as collection `110 Coliseum detailed front ruin`.

## Remaining work

Broad unequal facade value grouping is being studied separately as162/163. Other inherited folded meshes remain documented; successful local topology repairs do not certify every landmark mesh. No all-axis95 or final user acceptance claim.
