# 228 · Five curved Colosseum steps

Five native solid stone courses follow the existing foundation's evaluated front curve. They occupy its terrain-supported central arc (foundation samples37–95), with their ends behind the side architecture. Terrain outside the existing ground mesh is not extended.

Each tread is nominally0.95m deep, giving4.75m total run. Actual risers vary gradually with the foundation tilt, from0.350m to0.414m. These are broad monumental steps as requested. The highest tread matches the preserved foundation front elevation; the lowest courses embed35mm into the soil. All five meshes are watertight. A12mm worn arris and small unequal plan-edge variation keep the courses from looking mechanically perfect.

The material is the existing Colosseum foundation's painted masonry band, with transferred original-world paint coordinates. No gray placeholder or projected reference is used. New step geometry is included in the existing215 atmosphere-free native ink view map, with real tread-nose edges marked. No new render layer is introduced.

No prior object, original terrain, foundation, arch,225floor refit, descending tunnel, far-building placement, entrance, dust treatment, camera or character is changed. Existing110contact ink is checked against the evaluated new steps; it is retained when no new occlusion exists.

`tools/colosseum_steps_228.py::apply(scene)` is the reusable API. `audit.json` records fit and dimensions; `preservation-check.json` records fresh source/candidate comparison and visibility probes. Root owns the combined native render with the other current changes. CPU fit is not visual acceptance.
