# Held native material study 131

`tools/coliseum_surface_131.py::apply(C, strength=1.0)` on the local primary `110 Coliseum detailed front ruin` in source130. This is a separate candidate, not primary integration or user approval.

Retains the preferred held129 mineral matrix and four finite architecture-fed runoff fields. Deliberately omits the old129 core hue-ramp rewrite and bump-normal routing: accepted128 broad lighting, base palette, physical normals and glossy response remain unchanged. Copies42 source materials and reassigns only local landmark object slots. Explicitly excludes130 tunnel meshes and ink objects. No reference images or camera projection are used.

`geometry.blend` preserves130 source geometry and native ink. `baseline.png`, `candidate.png` and `candidate-gray.png` use the same camera, at native4K pixel density; image is a1340x910 crop of the landmark. `audit.json` records assignments and geometry identity. `classification.json` records existing used core-face counts; no classification is changed. U8L10 used corefaces, U8R167, sill10. Empty legacy slots are not evidence of actual fracture usage. Classification has not been inferred from radius-only tests.

References: UCL-01, UCL-02, UCL-11 (original project artwork by project creator), DP-03 (Darius Puia/BakaArts, reference only). Target properties: quiet broad masonry, selective mineral breakup within actual exposed core, connected localized stains below damage/ledges. Layout/geometry/color families are fixed.

Held for possible combination AFTER the user's131 architectural changes. Existing broad planar fracture geometry is not fixed by this material pass. New geometry/classification should precede this module if later integrated, and newcore materials must retain fracture role or Exposed masonry core naming.

## Inspection result
Modest local improvement, not a95 result. Central left broken return gains restrained mineral breakup, but large planar damaged surfaces remain simple. Main-frame difference is small:4167/1219400 pixels differ by>4RGB; mean absolute difference0.0355/255. Keep held until geometry work creates a stronger exposed-core foundation.
