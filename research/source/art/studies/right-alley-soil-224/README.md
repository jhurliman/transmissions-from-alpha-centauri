# 224 · Light soil in the right cross-alley

CPU candidate only; visual review belongs to the combined native render.

The removed brown plate is `Side road surface`, at world x 8–21 m, y 0.4–6 m, z 0–0.03 m. Existing `Street foundation` terrain beneath it is retained. A private material copy extends the accepted pale-bank color selector across that footprint, preserving the original selection everywhere else.

Six bank stones intersecting the cross-alley are hidden, and 44 small native fragments copied from the mid-left light-soil family are distributed in unequal loose groups with scattered isolated pieces. Maximum added stone dimension is 0.269 m. Their broad bases are seated on the actual terrain. They retain bank tags for the following 223 dust pass.

Fresh reopen comparison preserves all 17,203 existing object geometries and transforms. No original object is deleted; the only changes are the seven intended visibility flags and the foundation material binding. A scan for active Grease Pencil points along the old plate's top boundary found none. Final rendering should still check all contacts and foreground ink.

Apply `ground_floor_kit_222`, then `right_alley_soil_224.apply(scene)`, then bank dust 223. This pass never touches the new gate floor, main-road geometry, road stones, service assemblies, framing or selected characters.

See `audit.json`, `preservation-check.json`, `floor-inventory.json` and `config/right-alley-soil-224.json`. Reference intent is recorded there; no reference image is projected onto the scene.
