# 152 — Crown thickness and connected masonry age

Current retained working scene: `scene.blend`; reusable landmark collection: `kit.blend`; native output: `main-4k.png` (3840 × 2885). Compare against 150 in `prototype/review-152.html`. User approval remains pending.

This combines the locally reviewed 151V3 subtractive crown with its warm/violet core and the two unequal 152 facade-age groups. The crown has no constructive additions. Its exposed return is 61 × 37 native pixels, below the original 90-pixel breadth target; it is useful partial progress, not a silently relaxed scope gate. The facade fields use complete receiving-face masks and original object coordinates. They preserve deep niches, arch interiors and existing lighting.

Independent full-scene coliseum scores are 95 silhouette/scale, 95 structural depth, 92 damage plausibility, 93 surface richness, 93 painted integration and 94 linework. `critic.json` records the visual basis and remaining gaps. The 95-per-axis goal is unfinished.

Preservation compares 20,490 scene/instance objects with 150. Only two crown meshes change geometry/normals; 36 other receivers change material assignments. Existing material graphs, camera, lighting, transforms and unrelated geometry remain unchanged. Outside the landmark, the native renders differ by no more than 2 RGB units, with no pixels over 4. This is a measured small raster difference, not bit identity.

The latest alley treatment, including 3× bottom splatter and longer translucent rust tails, is carried forward. The native foreground ink visibility guard is also retained. A fresh reopen also confirms that all3,533 landmark objects in the kit match the scene under evaluated geometry, normals, transforms and material assignments; no linked IDs remain, and all nine full-scene file images are packed. The scene contains its render-pre script; trusted script execution is needed when rendering the saved scene directly. The versioned integration script applies the guard explicitly and does not require changing global Blender preferences.

Reproduction uses Blender 5.2.1 and the retained 150 scene. Run `tools/coliseum_integration_152.py` to build, then the same script with `-- render` to render. The script calls 151 geometry, 151 core and 152 facade modules and saves current native scene/kit plus preservation and generation reports. This is an incremental build, not a clean build from no historical inputs. Publish using `tools/publish_coliseum_152.py`; compare rendered pixels using `tools/coliseum_validate_152.py`.

References: UCL-01 (the project creator's original ChatGPT Images 2.5 artwork), UCL-02 detail crop and DP-03 (Darius Puia / BakaArts). Study properties are retained wall thickness, unequal connected age and quiet structural planes. No reference artwork is projected onto the scene.
