# U15R V2 — held after one bounded construction

The explicit corner-fan allowance repairs the branching boundary:13 enumerated source faces, including rear isolated remnant734, are removed with the inherited folded return. Four healthy opposing faces originally included in the crossing list were retained. The result has one312-edge boundary,1120 protected faces, no lost protected triangles, no nonmanifold edges and no zero-area faces.

The U10L ruled-strip correspondence does not produce a valid U15R cap. Float64 validation finds38 crossings:17 between preserved and reconstructed triangles,21 between reconstructed triangles. Thus this is not solely a remaining inherited defect. The interpolation crosses the concave stepped profile and cannot be accepted. New return vertices lie up to0.142547m from the original source surface; retained source geometry has zero coordinate movement. No tolerance was increased.

Next principled method, if resumed, is to partition this concave profile into its actual masonry step domains before constructing each return, using exact paired step corners as correspondence constraints rather than normalized total profile arclength. Each domain must meet the existing skin boundary without crossing the neighboring preserved wall. The current failed candidate must not be integrated.

All new intersections, world triangle coordinates and projected bounds are in shape-audit.json. Candidate.blend is diagnostic only. No GPU render, primary scene mutation, or frozen U10L/161 change occurred.
