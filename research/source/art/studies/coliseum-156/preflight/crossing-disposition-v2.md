#156 inherited crossings — float64 disposition v2

Read-only152 source. Audited43 objects: the34 legacy crossing receivers plus nine subsequently changed meshes. Evaluated mesh and original copied matrix are passed directly, preventing temporary-object shear decomposition. No rendered or saved geometry changes.

## Result

34 of43 candidates have2,291 float64-confirmed crossing pairs. All are within a single connected component, even on the two-component U10head; none of these pairs is explained by deliberate separate assembly overlaps.54 assembled round columns remain outside scope. Most old crossings are genuine folds, not float32 artifacts.

Band10 profile0's two legacy pairs are not confirmed. Conversely U9R contains five genuine pairs missed by the legacy float32 predicate. U15R increases from1,323legacy to1,397float64 pairs. The JSON records rejected and newly detected pairs perobject. Do not generalize this candidate-set total to a full float64 mesh certificate.

Current finite camera visibility checks find candidate intersections on sevenobjects. The largest projected segment is0.493px at4K (band10 profile4); U10head has40visible candidates, max0.315px. These are geometrically real, but this audit does not show a large visible rendering defect.6mm depth tolerance/three samples persegment can over- or underestimate practical visibility. Hidden folds can affect shadows and later viewpoints.

## Repair priorities

1. U10 aperture head and band10 profiles4/2: bounded current-camera return repairs. Preserve exact frontplanes, accepted apertures, silhouette and material coordinates; repair defective rear/side connectivity. U10head has156confirmed crossings. Band10profile2's tiny nonzero triangle is not removable as anexactzero.
2. U10L and U15R: native-asset quality backlog, not a new aesthetic scope. They retain292/1,397confirmed crossings and101/233collapsedfaces; no current visible samples. Bounded numerical cleanup failed because degenerate connectivity is entangled with folded returns. Reconstruct source return topology/boundary loops rather than relaxing tolerances, smoothing accepted faces or deleting arbitrary triangles.
3. U9R: retain the documented fivefold caveat through154; no currentvisiblecandidate. Earlier legacy-clean149reports are superseded for this predicate, not erased.

The other confirmed returns/crowns remain exactnamed backlog inJSON. Their existence should be disclosed without conflating them with a low visualscore or intentionalcolumnassembly. Five numerical cleanup targets remove14zeroarea faces only and do not repair thesefolds.

## Limits

Float64 narrowphase still uses a float32BVH broadphase, withoriginal10µm strict tolerances and exclusions forsharedvertices/coplanartriangles. It is a substantially better bounded classification, not exact arithmetic or an exhaustive allmesh proof. No GPUrender, primarymutation or fullcleanclaim.
