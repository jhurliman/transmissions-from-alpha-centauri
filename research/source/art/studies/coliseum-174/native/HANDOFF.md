#174 two-face native repair — CPU frozen, visual proof pending

The isolated candidate passes the complete native gates: U4-head robust crossings1→0, closed edge incidence, exact raw/evaluated topology correspondence, every vertex and named attribute preserved, every material slot unchanged, and all5476 protected evaluated triangles and corner normals exact. The127 layout modifier remains live and unchanged.

Only diagonal edge1413 changes304–83→1263–82. Generated connectivity differences are limited to .edge_verts[1413], .corner_vert[2133,13838] and .corner_edge[2133,2135,13837,13838]. All pre-existing non-topology attributes are exact; no new attributes were added. Source has no custom normals, so it remains automatic-normal geometry. Two changed hidden facets have geometric-normal changes~.558° and36.63°; protected evaluated corner-normal delta is0.

`repair_174.py::apply(C)` is a reusable, CPU-tested delta for a matching target. It copies the target mesh and changes only these two faces/edge; no append, modifier bake, global rebuild or material replacement. `candidate.blend` is an isolated168-based proof artifact, not an integration source to overwrite newer material work.

`proof_174.py` is prepared for the root-granted GPU slot. It renders matched168/candidate native4K crops[1440,440,1650,635], painted and actual-light clay, with Freestyle off equally and all native landmark ink retained. No render has been run yet. No crown carving or damage-score credit is implied.

## Completed matched proof

Painted and actual-light clay native210×195 crops both match168 exactly:0 changed pixels, maximum RGB difference0. All native landmark ink remained present; Freestyle was disabled equally. Both comparisons were visually inspected. GPU lane released. This is local preservation evidence and native topology quality only, with no damage-score claim. Replay the delta onto173/newer if retained; do not adopt the168 proof scene.
