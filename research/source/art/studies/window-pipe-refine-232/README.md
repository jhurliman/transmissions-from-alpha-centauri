# Window rhythm, broken glass and compact pipes · 232

The two broken panes now differ: a large jagged left sheet with a small opposite remnant, and a mostly-cleared opening retaining a jagged lower strip and narrow upper-side remnant. Every connected glass polygon reaches the actual frame rebate. The former detached near-corner triangles and hanging central stile are removed.

Existing native picture and sliding-window modules repeat at2.8m bay spacing on the same storey.32 new actual openings create30 groups, mostly pairs, with three windows across both stepped street facades. Return-wall structural backings receive true Boolean openings. Existing regular gallery windows remain. New sibling windows are intact; there are still exactly two broken instances.

Two visible tall-prefab services change: left83 uses existing native L880×600 rectangular alley duct modules; right89 uses two native600mm straight barrels plus a low maintenance housing, with close-wall vertical docking receivers. Neither motif uses the former projecting C bends. Both routes sit on the inter-bay pier, with the wider round housing below the glazing and its top receiver above. All endpoints are seated in fitted receivers/plinths, with short wall supports. Original component geometry and materials remain unchanged in private instance copies. The new232 roof equipment is preserved.

API `tools/window_pipe_refine_232.py::apply(scene)`. Root integrates before final landmark fractures, then performs the common110 visibility reclip while preserving225 lost-sill cleanup. CPU preservation/visibility evidence is recorded separately; parent owns actual camera rendering and visual review.
